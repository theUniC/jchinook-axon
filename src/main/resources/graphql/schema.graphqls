type Query {
    artists(offset: Int, limit: Int): [Artist]
    artist(id: ID!): Artist
}

type Mutation {
    createArtist(artistName: String!): Artist
}

type Artist {
    id: ID!
    name: String!
}